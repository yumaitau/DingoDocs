/* eslint-disable @typescript-eslint/no-require-imports */
// Injected so CommonJS runtime bundles can safely resolve import.meta.url.
export const import_meta_url =
  require("node:url").pathToFileURL(__filename).href;
